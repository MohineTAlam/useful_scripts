# useful_scripts
Scripts that could be useful in the OPTRA pipeline:

install_nextflow_stack.sh = set up nextflow, docker, and conda, and their dependencies to be able run OPTRA pipeline
samplesheet_checks_prep.py = quickly check if samplesheet is in the correct format and edit values that are incorrect or missing
